<div>
  <% @order.order_items.each do |item| %>
    <p><%= item.name %> - Quantidade: <%= item.quantity %> - Preço: <%= number_to_currency(item.total_price / 100.0, unit: "R$") %></p>
  <% end %>
  <h4>Total: <%= number_to_currency(@order.total_price / 100.0, unit: "R$") %></h4>
</div>
